
return {
  "nvim-tree/nvim-web-devicons",
  config = function()
    require("nvim-web-devicons").setup({
      -- your devicon config here
      -- for example, you can set the default icon for unknown files
      -- default = true,
    })
  end,
}

